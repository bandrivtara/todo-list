import React from 'react';
import {LoginContainer} from "./Login/LoginContainer";

export const Auth = () => {
    return (
        <div className="auth-card">
            <LoginContainer/> 
        </div>
    );
};
